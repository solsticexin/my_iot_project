```mermaid
flowchart TD
    A[显示任务启动] --> B[初始化显示屏]
    B --> C{初始化成功？}
    C -->|否 | D[记录错误并退出]
    C -->|是 | E[绘制静态中文标签]
    E --> F[从全局变量读取传感器数据]
    F --> G[格式化数值为字符串]
    G --> H[刷新数值显示区域]
    H --> I[延时 3 秒]
    I --> F
```
