# 通信协议

本系统通过 UART 串口与上位机或其他外部设备进行通信，采用 JSON 格式进行数据封装，波特率为 115200，数据位 8 位，停止位 1 位，无校验位。

## 1. 报文总览

| 报文类型 | 方向 | 功能说明 | 触发条件 |
|----------|------|----------|----------|
| 数据上报 | 设备→上位机 | 上报传感器数据和执行器状态 | 周期性发送，约 2 秒/次 |
| 命令下发 | 上位机→设备 | 控制执行器开关或脉冲 | 用户手动发送 |
| 命令回复 | 设备→上位机 | 回复命令执行结果 | 收到有效命令后立即回复 |

## 2. 数据上报

设备周期性上报当前传感器采集数据和执行器状态，报文格式如下：

| 字段名 | 类型 | 说明 | 取值范围 |
|--------|------|------|----------|
| type | string | 报文类型，固定为"data" | "data" |
| temp | float | 温度值（℃） | 0~50 |
| humi | float | 湿度值（%RH） | 0~100 |
| soil | uint16 | 土壤湿度原始 ADC 值 | 0~4095 |
| lux | float | 光照强度（lx） | 0~65535 |
| water | uint8 | 水阀状态 | 0=关闭，1=开启 |
| light | uint8 | 灯光状态 | 0=关闭，1=开启 |
| fan | uint8 | 风扇状态 | 0=关闭，1=开启 |
| buzzer | uint8 | 蜂鸣器状态 | 0=关闭，1=开启 |

**数据上报示例：**

```json
{"type":"data","temp":25.0,"humi":60.0,"soil":350,"lux":500.0,"water":0,"light":1,"fan":0,"buzzer":0}
```

## 3. 命令下发

上位机通过发送命令控制设备的执行器，报文格式如下：

| 字段名 | 类型 | 说明 | 取值范围 |
|--------|------|------|----------|
| type | string | 报文类型，固定为"cmd" | "cmd" |
| target | string | 控制目标设备 | "water"/"fan"/"light"/"buzzer" |
| action | string | 控制动作 | "on"/"off"/"pulse" |
| time | uint64 | 脉冲持续时间（秒） | 0~300，仅 pulse 动作有效 |

**动作说明：**

| 动作 | 说明 | time 字段 |
|------|------|-----------|
| on | 开启设备 | 忽略 |
| off | 关闭设备 | 忽略 |
| pulse | 脉冲模式，开启后自动关闭 | 有效，最大 300 秒 |

**命令下发示例：**

```json
{"type":"cmd","target":"water","action":"on","time":0}
{"type":"cmd","target":"fan","action":"pulse","time":10}
{"type":"cmd","target":"light","action":"off","time":0}
```

## 4. 命令回复

设备收到有效命令后回复执行结果，报文格式如下：

| 字段名 | 类型 | 说明 | 取值范围 |
|--------|------|------|----------|
| type | string | 报文类型，固定为"ack" | "ack" |
| target | string | 目标设备 | "water"/"fan"/"light"/"buzzer" |
| action | string | 执行的动作 | "on"/"off"/"pulse" |
| result | string | 执行结果 | "ok"/"error" |

**命令回复示例：**

```json
{"type":"ack","target":"water","action":"on","result":"ok"}
{"type":"ack","target":"fan","action":"pulse","result":"ok"}
```

## 5. 错误处理

| 错误场景 | 设备行为 |
|----------|----------|
| 收到未知 target | 记录日志，忽略该命令 |
| 收到未知 action | 记录日志，忽略该命令 |
| time 超过 300 秒 | 自动限制为 5 秒 |
| JSON 解析失败 | 记录日志，忽略该报文 |
| 串口发送失败 | 记录日志，继续下一次发送 |
